---
name: daily-triage
description: Surface every transaction that needs attention today, ranked overdue-first with rationales per row.
---

Invokes the `daily-triage` skill. Single tool call to `docjacket.get_next_required_actions` returns the full pre-ranked feed. See `skills/daily-triage.md` for the rendering rules.
